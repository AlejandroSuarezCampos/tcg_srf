<?php
/**
 * Fusionada en `acceso.php` (bloque 7a del rediseño).
 *
 * Se conserva como redirección: la portada enlaza aquí desde sus dos botones.
 */
header('Location: acceso.php?modo=crear', true, 301);
exit;
